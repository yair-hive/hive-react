import maps from './maps'
import seats from './seats'
import guests from './guests'
import tags from './tags'
import map_elements from './map_elements'
import projects from './projects'
import guest_groups from './guest_groups'
import seat_belongs from './seat_belongs'
import tag_belongs from './tag_belongs'
import requests_belongs from './requests_belongs'

const new_api = {
    maps,
    seats,
    guests,
    tags,
    map_elements,
    projects,
    guest_groups,
    seat_belongs,
    tag_belongs,
    requests_belongs,
}

export default new_api